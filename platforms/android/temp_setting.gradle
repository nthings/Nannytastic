android {
            defaultConfig {
                applicationId = "org.nativescript.nanys"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }